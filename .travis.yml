language: python
python:
  - "2.7"
  - "3.6"
# command to install dependencies
script:
  - python api_test.py
  - python test.py
  - coverage run test.py
branches:
  only:
    - master
install:
  - pip install codecov
  - pip install -r requirements.txt
after_success:
- codecov